# # This file was auto-generated by the Firebase CLI
# # https://github.com/firebase/firebase-tools

# name: Deploy to Firebase Hosting on merge
# 'on':
#   push:
#     branches:
#       - main
# jobs:
#   build_and_deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '14' # or any version you prefer
#       - name: Install Angular CLI
#         run: npm install -g @angular/cli
#       - name: Install dependencies
#         run: npm install --force
#       - name: Build Angular App
#         run: ng build
#       - uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: '${{ secrets.GITHUB_TOKEN }}'
#           firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_MYINVOICE_E4E41 }}'
#           channelId: live
#           projectId: myinvoice-e4e41
#         env:
#           FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}



# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
        with:
          node-version: '14' # or any version you prefer
      - name: Install Angular CLI
        run: npm install -g @angular/cli
      - name: Install dependencies
        run: npm install --force
      - name: Build Angular App
        run: ng build
      - name: firebase Set Production
        run: firebase target:apply hosting production mycustomer
      - name: firebase Deploy Production 
        run: production

